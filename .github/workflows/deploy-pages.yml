name: Deploy learning site to GitHub Pages

on:
  push:
    branches: ["claude/ai-engineering-learning-repo-fbmgw7"]
    paths:
      - "html/**"
      - "scripts/build_html.py"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Rebuild HTML from the markdown source
        run: |
          python -m pip install --quiet markdown pygments
          python scripts/build_html.py

      - name: Make the site root land on index.html
        run: cp html/index.html html/404.html || true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./html

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
